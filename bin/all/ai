#!/usr/bin/env sh

if [ "$(dirname "$(pwd)")" = "$HOME/.local/state/sessions" ]; then
    if [ ! -d .git ]; then
        git init
    fi
fi

export ANTHROPIC_ENABLE_1M_CONTEXT=true

exec opencode --continue
# exec claude --continue
