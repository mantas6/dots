#!/usr/bin/env sh

set -e

name='lt.utf-8.spl'
dir="$HOME/.config/nvim/spell"

mkdir -p "$dir"
cd "$dir"

[ -f "$name" ] && exit 0

curl -fsSL "https://ftp.nluug.nl/pub/vim/runtime/spell/$name" -o "$name"
