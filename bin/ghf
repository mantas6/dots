#!/usr/bin/env sh

gh api user/starred --cache 48h --paginate --jq '.[] | "\(.full_name):\(.description)"' \
    | column -ts ':' \
    | fzf \
    | cut -d' ' -f1 \
    | xargs -I {} gh repo view {} --web > /dev/null
