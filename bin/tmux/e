#!/usr/bin/env sh
# Choose tmux session

session="$(
    sessionizer | fzf-tmux -p 60%,50% \
        --accept-nth='{2..}' \
        --layout=reverse \
        --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
        --header '  ^d kill' \
        --bind 'tab:down,btab:up' \
        --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)'
)"

[ -n "$session" ] && sessionizer "$session"

exit 0
