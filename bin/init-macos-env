#!/usr/bin/env sh

[ "$(uname)" != 'Darwin' ] && exit 1

[ ! -x "$(command -v php)" ] && /bin/bash -c "$(curl -fsSL https://php.new/install/mac/8.4)"

defaults write -g InitialKeyRepeat -int 10
defaults write -g KeyRepeat -int 1
# defaults write -g InitialKeyRepeat -int 15
