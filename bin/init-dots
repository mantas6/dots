#!/usr/bin/env sh
# Initialize additional packages/repositories

for pkgDir in "$DOTS_DIR"/lib/*; do
    (cd "$pkgDir" && [ -f build ] && ./build)
done

cp -v "$DOTS_DIR/conf/stubs/hooks/dots-pre-commit" "$DOTS_DIR/.git/hooks/pre-commit"
