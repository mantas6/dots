#!/bin/sh

dir=$(dirname "$(readlink -f "$0")")

# Create base directories so that stow wouldn't link them directly
xargs -a "$dir/directories" -I {} mkdir -pv "$HOME/{}"

stow --dir="$dir" --target="$HOME" --verbose base "$@"
