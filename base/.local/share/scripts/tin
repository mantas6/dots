#!/bin/sh

root=$(pwd)
file=$(mktemp "$root/.XXXXXXX.php")

echo '<?php' >> "$file"
echo "require __DIR__.'/vendor/autoload.php';" >> "$file"

nvim -c "set splitright | autocmd BufWritePost * only | vsp | terminal de php $(basename "$file")" "$file"

rm "$file"
