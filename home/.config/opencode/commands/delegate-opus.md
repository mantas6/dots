---
name: delegate-opus
agent: build
---

Execute current plan in separate opus subagents / todos. Commit after each, push when all is done + execute `sat-notify "{} done"`, replace `{}` with repo name.
